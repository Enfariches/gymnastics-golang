1. Не используется модуль handlers
2. Не используется модуль paginate.go (900 строк кода)
3. Нет обработчиков для таблицы users
4. Нет корректное добавление в таблицу stat. [statsController:44], а stat взаимодействуюет с таблицей users [statsController:69]
5. Нет добавления в таблицу admins, из-за чего нельзя пользоваться API.